# 自然语言查询（NLQ）人工评测协议

本文档与 `doc/paper/main.tex` 中表 5-1（`tab:nlq-category`）对应。**请勿**将 API Key 写入本文件。

当前图谱为 **Neo4j 多标签模型**，**不包含**旧版 GDB 方案中的 PLACE、SUMMON 桥接关系；评测问句应围绕现库关系（CONSUMES、PRODUCES、DROPS、TOOLMINEDROPS 等）设计。

## 1. 环境与模型（每次评测需记录）

| 项目 | 填写 |
|------|------|
| 评测日期 |26-04-06|
| `LLM_API_BASE` | （域名即可，勿写密钥） |
| `LLM_MODEL` | |
| 温度 / 其它采样参数 | |
| Neo4j 数据批次 / commit | |
| 后端 commit | `git rev-parse --short HEAD` |

## 2. “通过”判定标准

在**不修改**用户自然语言问句的前提下，同时满足：

1. `POST /nlq` 返回 HTTP 200。
2. 返回体中 Cypher 通过 `cql_validator`（与线上一致）。
3. 查询在超时与行数限制内执行成功。
4. 返回的 `graph` 或 `table` **在语义上能回答问句**（答非所问则记未通过）。

## 3. 21 条用例：模板问句（【】内由你换成真实道具/生物名）

下表**第二列**为建议句式，请把 `【…】` 换成你库里存在的名称后，在网页上逐条执行；**第三列**填通过/未通过；**第四列**可写备注。重测后若与论文表 5-1 数字不一致，**请同步改论文**。

### 道具查找（4）

| 序号 | 建议问句（把【】替换为真实名称） | 结果 | 备注 |
|------|----------------------------------|------|------|
| 道具-1 | 查找石剑 | MATCH path = (n:item) WHERE n.Name CONTAINS $name AND NOT n:monster AND NOT n:block RETURN path LIMIT 100，查询成功，一堆带有石剑的道具节点| 纯 `:item` 模糊名 |
| 道具-2 | 搜一下火炬 | MATCH path = (n:item) WHERE n.Name CONTAINS $name RETURN path LIMIT 100 ，查询成功| |
| 道具-3 | 铝棒是什么东西 | 只查到了一个叫铝棒的东西，没有它的关系（好像也没有要求关系）| |
| 道具-4 | 有没有叫法杖的物品 |查询成功 正确 查到了一堆法杖及其融合道具 都没有关系 |  |

### 合成查询（3）

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 合成-1 | 木料能合成什么 | 查询成功，生成的cql正常，查到了木料及其配方和产物| 原料→配方→产物链 |
| 合成-2 | 怎么合成工匠台 | 查询成功且结果正确 | 产物→配方→原料 |
| 合成-3 | 铜镐的合成配方是什么 | 查询成功，查到了黄铜镐的合成和融合配方| |

### 掉落查询（3）

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 掉落-1 | 击败野人会掉落什么 | 查询成功且结果正确，查到了所有名字中含有野人的掉落关系 | `:item:monster` + DROPS |
| 掉落-2 | 打死虚空使徒给什么 | 查询成功且结果正确，查到了它掉落的创造晶体| 换你库中存在的生物名 |
| 掉落-3 | 爆爆蛋会掉什么材料 | 查询成功且结果正确| |

### 方块查询（3）

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 方块-1 | 深积岩有哪些掉落 | MATCH path = (n:item:block)-[:TOOLMINEDROPS]-(drop:item) WHERE n.Name CONTAINS $name RETURN path LIMIT 100，只查到了深积岩这一个方块节点，没有正常返回其掉落物 | TOOLMINEDROPS |
| 方块-2 | 钛合金矿怎么挖 |在图上看不到工具，但在结果的json中可以看到tool的类型和level | |
| 方块-3 | 挖钨矿推荐什么镐 | 查到了钨矿，同上，只能在结果的json中看到| XX 换常见矿石方块 |

### 生物查询（2）

> 已取代原“召唤查询”：现库无 SUMMON，意图落在 **怪物/生物本体 + 属性或掉落**，勿再写“召唤兽”“刷怪蛋召唤”等依赖旧关系的说法。

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 生物-1 | 野人的生命和攻击是多少 | 查到了野人和它的掉落关系，但是也能在json中看到野人的属性 | `Life`/`Attack` 或表格展示 |
| 生物-2 | 查一下生物雀莺的属性 | 查到两个生物 在json中可看到属性| |

### 关系 / 分组探索（2）

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 关系-1 | 树枝属于哪个分组 | 正确查到了树枝和它关联的分组| IN_GROUP（若数据中有） |
| 关系-2 | 和冶炼台分组有关的物品有哪些 | MATCH path = (item:item)-[:IN_GROUP]-(group:group) WHERE group.ID = '冶炼台' RETURN path LIMIT 100，不正确，因为group的id不是中文字而是主物品的id，这边应该要从冶炼台这个道具查起与它有关的分组与和该分组有关的物品 大概是复杂查询了| 可放宽，看模型能否用到 group |

### 复杂查询（2）

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 复杂-1 | 所有能合成颜料瓶的原料有哪些 | 结果正确 | 多跳 |
| 复杂-2 | 哪些武器可以通过工匠台合成出来 | 失败, MATCH path = (product:item)-[:PRODUCES]-(r:recipe)-[:CONSUMES]-(material:item) WHERE r.StationLabel = '工匠台' RETURN path LIMIT 100，但是工匠台关系应该用id而不是名字| 开放类，易失败，作压力样本 |

### 边缘情况（2）

| 序号 | 建议问句 | 结果 | 备注 |
|------|----------|------|------|
| 边缘-1 | 查找地心门砖 | 无结果 | 应优雅空结果或合理 Cypher |
| 边缘-2 | 石剑和铜矛的合成材料有什么不同 | 生成应该不正确，查询失败，llm忘记了模糊匹配 MATCH path = (material1:item)-[:CONSUMES]-(recipe1:recipe)-[:PRODUCES]-(product1:item {Name: $name1}) MATCH path2 = (material2:item)-[:CONSUMES]-(recipe2:recipe)-[:PRODUCES]-(product2:item {Name: $name2}) RETURN path, path2 LIMIT 100| 多实体对比 |

---

### 汇总到论文表 5-1（已回填 `main.tex`）

| 论文中的类别 | 通过/总数 | 准确率 |
|--------------|-----------|--------|
| 道具查找 | 4/4 | 100% |
| 合成查询 | 3/3 | 100% |
| 掉落查询 | 3/3 | 100% |
| 方块查询 | 2/3 | 66.7% |
| 生物查询 | 2/2 | 100% |
| 关系查询 | 1/2 | 50.0% |
| 复杂查询 | 1/2 | 50.0% |
| 边缘情况 | 1/2 | 50.0% |
| **合计** | **17/21** | **81.0%** |

## 4. 与 pytest 的边界

- `tests/test_api.py` 中 `/nlq` **Mock 了 LLM**，不代表上表通过率。
- 自动化测试 38 条与上表 21 条**相互独立**。

## 5. 验收说明（按协议 §2 四条标准）

对“结果”列文字逐条核定如下（**通过** / **未通过**）。

| 序号 | 判定 | 简要理由 |
|------|------|----------|
| 道具-1～4 | 通过 | 返回能支撑“查找/是否存在”类意图；法杖等融合名 CONTAINS 合理。 |
| 合成-1～3 | 通过 | 配方链或产物—原料链与问句一致。 |
| 掉落-1～3 | 通过 | 生物—掉落路径与问句一致。 |
| 方块-1 | **未通过** | 问“掉落”却只回到方块节点，未展开掉落关系，语义不完整。 |
| 方块-2、3 | 通过 | “怎么挖/推荐镐”在 JSON 中可见工具与等级，视为已回答问题。 |
| 生物-1、2 | 通过 | JSON 中可见 Life/Attack 等属性，满足“属性是多少”类意图。 |
| 关系-1 | 通过 | 树枝—分组正确。 |
| 关系-2 | **未通过** | `group.ID` 误用中文名，与 Schema 不符，结果不正确。 |
| 复杂-1 | 通过 | 颜料瓶原料多跳正确。 |
| 复杂-2 | **未通过** | StationLabel 与数据键语义不一致导致失败（与备注一致）。 |
| 边缘-1 | 通过 | 无结果属合理空答（或库中无该物）。 |
| 边缘-2 | **未通过** | 查询失败 / 未坚持 CONTAINS 模糊匹配。 |

**请在 §1 补全 `LLM_MODEL` 与 commit**，便于他人完全复现本次 81.0\%。
